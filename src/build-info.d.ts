/** Injected by build.js (esbuild `define`) — a per-build identifier
 *  surfaced in Settings → Help so users can verify which version
 *  their bookmarklet is currently running. */
declare const __BUILD_ID__: string;
